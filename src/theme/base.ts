// Central "global" brand colors (default theme).
// Keep this tiny: it's used by layout metadata (theme-color) and defaults.

export const NCX_BRAND = {
  // Used for <meta name="theme-color">. This should be a solid color (not a CSS var).
  themeColor: "#ff00ff",
};
